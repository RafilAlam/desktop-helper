#include <iostream>
using namespace std;

int main() {
    cout << "Insert Augend: ";
    int augend;
    cin >> augend;

    cout << "\nInsert Addend: ";
    int addend;
    cin >> addend;

    printf("%d + %d = %d\n", augend, addend, augend + addend);
    return 0;
}
